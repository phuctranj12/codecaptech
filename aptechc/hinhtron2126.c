#include<stdio.h>
#include<math.h>
int main(){
    float pi = 3.14159;
    float a;
      scanf("%f",&a);
    float chuvi = pi*2*pi*a;
    float dientich = pi*pi*a;
    printf("%.2f\n",dientich);
    printf("%.2f",chuvi);
}